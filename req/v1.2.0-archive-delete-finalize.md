1. Define deletion semantics explicitly in req/v1.2.0-archive-delete.md
  - After retention expiry, fiscal-year deletion removes DB rows and stored files.
  - Audit log rows tied only to the deleted fiscal year are permanently deleted, not archived.
  - Company deletion is allowed only when no fiscal years remain, then removes remaining company-scoped non-accounting metadata.
2. Move stored-file deletion into FiscalYearDeletionService
  - Delete attachment/report files as part of deleteFiscalYear(), not only in FiscalYearDeletionDialog.
  - Keep DB deletion transactional, but treat file deletion as a controlled follow-up with clear reporting.
  - Return a deletion result with deletedFiles, failedFiles, and purge counts.
  - If any file cannot be deleted, report it explicitly so the UI/MCP can surface manual cleanup.
3. Replace audit-log archiving for permanent deletion
  - Add a new method, e.g. deleteFiscalYearAuditLogRows(sql, companyId, fiscalYearId).
  - Delete audit rows referencing the fiscal year, its periods, vouchers, or attachments.
  - Rebuild/reset audit_log_chain_head afterward so remaining audit rows validate.
  - Keep existing archive behavior only for non-permanent replacement workflows if still needed.
4. Fix deletion ordering
  - Collect attachment/report storage paths first.
  - Delete dependent audit rows first.
  - Delete report archives, attachments, opening balances, closing entries, VAT periods, vouchers, voucher series, then fiscal year.
  - Delete files after DB commit, using the collected paths.
5. Update callers
  - FiscalYearDeletionDialog should stop calling deleteStoredFilesQuietly() directly and instead render file cleanup failures from the service result.
  - Any MCP or service callers should receive the same deletion result.
6. Add tests
  - Fiscal-year deletion removes audit rows permanently.
  - Remaining audit chain validates after deletion.
  - Fiscal-year deletion deletes attachment/report files when called directly through the service.
  - File deletion failure is reported and does not silently pass.
  - Retention window still blocks deletion before seven years.
  - Company deletion succeeds only after all fiscal years are gone.
7. Verify
  - Run focused tests for FiscalYearDeletionServiceTest, CompanyServiceTest, and audit integrity.
  - Run ./gradlew spotlessApply.
  - Run full ./gradlew build.