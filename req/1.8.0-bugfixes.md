# v1.8.0 — Bugfixar: räkenskapsår-kombobox, AI-föreslagen verifikation (bilaga, datum, navigering)

## Context

Tre buggar rapporterade av användaren:

1. Efter att ha skapat ett nytt räkenskapsår går det inte att välja det — komboboxen längst ner (i `MainFrame`) uppdateras inte till det nya året.
2. Vid skapande av en verifikation från en PDF (AI-assisterat flöde via MCP) syns inte bilagan i fliken "Bilagor" på den föreslagna verifikationen, och datumet fylls inte i korrekt.
3. Vid bläddring tillbaka bland tidigare verifikationer försvann en ännu ej sparad verifikation när användaren bläddrade tillbaka till den.

Tre parallella kodgranskningar (`MainFrame`/`FiscalYearPanel`, MCP-utkastflödet i `VoucherPanel`, samt navigerings-/utkasthanteringen i `VoucherPanel`/`VoucherNavigation`) plus uppföljande manuell läsning av källkoden gav konkreta, kodbekräftade rotorsaker för bugg 1, 2a och 3. Ett befintligt "kom-ihåg-utkast"-skydd (commit `8a5309b`) täcker redan den vanliga bläddra-fram-och-tillbaka-scenariot (test `preservesUnsavedDraftWhenNavigatingToSavedVouchersAndBack` i `VoucherPanelNavigationTest.groovy` går grönt) — bugg 3 visade sig vara ett specifikt hål i det skyddet som bara triggas via MCP-utkastflödet, inte en generell brist.

**Granskningsrunda 2 (extern granskare) hittade tre luckor i den första versionen av detta dokument, alla verifierade och åtgärdade:** (1) bugg 1 adresserade bara komboboxens läsning, inte att `ActiveCompanyManager.reloadFiscalYears()` väljer fel år för ett bakåtdaterat räkenskapsår — se 1a nedan; (2) fix 2a testade bara navigering mellan redan sparade verifikationer, inte det vanliga spara-med-bilaga-flödet med standardinställningar — se "Designbeslut" under 2a; (3) fix 2b har nu ett explicit DatePicker-intervall och MCP-synlig validering, men den ursprungliga rapporten om ett datum inom räkenskapsåret är fortfarande inte reproducerad — se "Beslut" under 2b.

## Fynd och åtgärder

### 1. Nytt räkenskapsår väljs inte i komboboxen

**Filer:**
- `app/src/main/groovy/se/alipsa/accounting/ui/MainFrame.groovy:741-767` (`reloadFiscalYearComboBox`)
- `app/src/main/groovy/se/alipsa/accounting/ui/ActiveCompanyManager.groovy:115-119` (`reloadFiscalYears`)
- `app/src/main/groovy/se/alipsa/accounting/ui/FiscalYearPanel.groovy:265` (`createFiscalYearRequested`)

Bugg 1 har två oberoende, samverkande orsaker. Flödet vid skapande är: `FiscalYearPanel.createFiscalYearRequested()` → `activeCompanyManager.reloadFiscalYears()` → `setFiscalYear(...)` → `PropertyChangeEvent(FISCAL_YEAR_PROPERTY)` → `MainFrame.propertyChange()` → `SwingUtilities.invokeLater { onFiscalYearChanged() }` → `reloadFiscalYearComboBox()`.

**1a — `ActiveCompanyManager.reloadFiscalYears()` väljer inte nödvändigtvis det nyss skapade året:**

```groovy
115  void reloadFiscalYears() {
116    List<FiscalYear> years = listFiscalYears()
117    FiscalYear newYear = years.isEmpty() ? null : years.first()
118    setFiscalYear(newYear)
119  }
```

`listFiscalYears()` (→ `FiscalYearService.listFiscalYears`, `FiscalYearService.groovy:58-75`) sorterar `order by start_date desc`, så `years.first()` är räkenskapsåret med **senaste startdatum bland alla existerande år** — inte nödvändigtvis det som precis skapades. `FiscalYearPanel.createFiscalYearRequested()` (rad 259-266) skickar aldrig det nya årets id vidare till `activeCompanyManager`:

```groovy
259  FiscalYear year = fiscalYearService.createFiscalYear(activeCompanyManager.companyId, nameField.text, startDate, endDate)
260  clearInputs()
261  reloadData()
262  selectFiscalYear(year.id)          // enbart FiscalYearPanels EGEN tabellrad-markering, se rad 402-410 — ingen koppling till activeCompanyManager
263  maybeTransferOpeningBalances(year)
264  activeCompanyManager.reloadFiscalYears()   // vet inte vilket år som skapades
265  showInfo(I18n.instance.format('fiscalYearPanel.message.created', year.name))
```

`selectFiscalYear(Long)` (rad 402-410) manipulerar bara `fiscalYearTable`s radmarkering i `FiscalYearPanel`s egen lista — helt frikopplat från `activeCompanyManager`. Nettoeffekt: om ett användare skapar ett räkenskapsår som **inte** har det senaste startdatumet av alla befintliga år för bolaget (t.ex. ett bakåtdaterat år för att fylla en lucka, eller helt enkelt om ett senare år redan finns), väljer `reloadFiscalYears()` tyst fel år — bugg 1 kvarstår även efter fixen i 1b nedan, eftersom `activeCompanyManager.fiscalYear` själv redan pekar fel innan komboboxen ens läser det.

**Åtgärd 1a:** ge `reloadFiscalYears()` en valfri, föredragen id-parameter (samma mönster som `VoucherPanel.refreshSeriesComboBox(preferredCode)` och `MainFrame.selectCompanyInComboBox(id)`), och skicka det nyss skapade årets id från `FiscalYearPanel`:

```groovy
void reloadFiscalYears(Long preferredFiscalYearId = null) {
  setFiscalYear(resolveInitialFiscalYear(listFiscalYears(), preferredFiscalYearId))
}
```

```groovy
activeCompanyManager.reloadFiscalYears(year.id)
```

(byt bara rad 264 i `FiscalYearPanel.groovy`; standardvärdet `null` håller `ActiveCompanyManager.groovy:82`s interna anrop och `FiscalYearPanel`s tre andra anrop — stäng/återöppna/ta bort räkenskapsår, rad 310/355/371 — oförändrade i beteende. De tre anropen har ett liknande, men mindre akut, potentiellt problem: om det aktiva året stängs/återöppnas/tas bort väljer `reloadFiscalYears()` fortfarande "senaste startdatum" istället för att i första hand behålla samma år (om det fortfarande finns). Detta rapporterades inte som en bugg och åtgärdas inte här — se "Explicit utanför scope".)

**Test** (utökar befintlig `app/src/test/groovy/unit/se/alipsa/accounting/ui/ActiveCompanyManagerTest.groovy`, som redan använder `DatabaseService.newForTesting()` och är den testbara nivå där denna bugg faktiskt lever — `FiscalYearPanel`s skapa-dialog är en blockerande modal `JOptionPane` och `MainFrame` går inte att instansiera i tester, se 1b nedan):

```groovy
@Test
void reloadFiscalYearsPrefersAnExplicitlyCreatedYearOverTheOneWithTheLatestStartDate() {
  FiscalYear latestByDate = fiscalYearService.createFiscalYear(
      CompanyService.LEGACY_COMPANY_ID, '2026', LocalDate.of(2026, 1, 1), LocalDate.of(2026, 12, 31))
  ActiveCompanyManager manager = new ActiveCompanyManager(companyService, fiscalYearService)
  manager.reloadFiscalYears()
  assertEquals(latestByDate.id, manager.fiscalYear.id)

  FiscalYear backdatedYear = fiscalYearService.createFiscalYear(
      CompanyService.LEGACY_COMPANY_ID, '2024', LocalDate.of(2024, 1, 1), LocalDate.of(2024, 12, 31))

  manager.reloadFiscalYears(backdatedYear.id)

  assertEquals(backdatedYear.id, manager.fiscalYear.id)
}

@Test
void reloadFiscalYearsWithoutAPreferredIdStillPicksTheLatestByStartDate() {
  fiscalYearService.createFiscalYear(
      CompanyService.LEGACY_COMPANY_ID, '2024', LocalDate.of(2024, 1, 1), LocalDate.of(2024, 12, 31))
  FiscalYear latest = fiscalYearService.createFiscalYear(
      CompanyService.LEGACY_COMPANY_ID, '2026', LocalDate.of(2026, 1, 1), LocalDate.of(2026, 12, 31))
  ActiveCompanyManager manager = new ActiveCompanyManager(companyService, fiscalYearService)

  manager.reloadFiscalYears()

  assertEquals(latest.id, manager.fiscalYear.id)
}
```

Kör testerna innan fixen → första testet rött (`manager.fiscalYear.id` förblir `latestByDate.id` efter `reloadFiscalYears(backdatedYear.id)` eftersom parametern inte finns/används än). Applicera fixen → grönt. Andra testet är en regressionsspärr som ska vara grönt både före och efter.

`FiscalYearPanel.groovy:264`s enradsändring (att faktiskt skicka `year.id`) verifieras inte av ett eget `FiscalYearPanel`-test i denna omgång — att driva den blockerande `showCreateFiscalYearDialog()`-modalen i test är samma typ av friktion som tidigare ledde till att en analog `FiscalYearPanel`-lokaliseringsfix medvetet drogs ur en tidigare plan (`docs/superpowers/plans/2026-07-31-mcp-and-locale-fixes.md`, avsnittet "Dropped: FiscalYearPanel"). Wiring-raden är triviell (en parameter läggs till ett redan existerande anrop) och verifieras via `./gradlew build` plus manuell kodgranskning — notera detta som en medveten testtäckningslucka, av samma typ som MainFrame-luckan som dokumenteras i 1b nedan.

**1b — komboboxen läser inte `activeCompanyManager.fiscalYear` korrekt ens när den är rätt:**

Roten till den delen av buggen är rad 745:

```groovy
FiscalYear selected = (fiscalYearComboBox.selectedItem as FiscalYear) ?: activeCompanyManager.fiscalYear
```

`fiscalYearComboBox.selectedItem` är i praktiken *aldrig* `null` efter första gången ett räkenskapsår valts — den behåller alltid en referens till vad som senast var valt i UI:t. Elvis-operatorn faller därför nästan aldrig tillbaka till `activeCompanyManager.fiscalYear`, utan hittar och återväljer det **gamla** året i den nyladdade listan (loopen på rad 753-760 matchar på `item.id == selected.id`). Nettoeffekten: `activeCompanyManager.fiscalYear` pekar internt på det nya året (resten av appen, t.ex. `VoucherPanel`, ser rätt år), men komboboxen visar fortfarande det gamla — tills användaren manuellt öppnar den och byter.

Motsvarande mönster finns redan korrekt löst för `companyComboBox` (`MainFrame.groovy:951-955` + `selectCompanyInComboBox(Long)`, rad 724-730): vid skapande av ett nytt bolag anropas explicit `selectCompanyInComboBox(saved.id)` med det nyss skapade objektets id som *auktoritativ* källa, inte komboboxens tidigare val.

**Åtgärd:** byt prioritetsordning på rad 745 så att `activeCompanyManager.fiscalYear` (den auktoritativa "aktiva" statusen) vinner över komboboxens egna, potentiellt inaktuella `selectedItem`:

```groovy
FiscalYear selected = activeCompanyManager.fiscalYear ?: (fiscalYearComboBox.selectedItem as FiscalYear)
```

Detta är en enradsfix i linje med redan använt mönster (auktoritativ modell-state vinner över UI-widgetens egen minnesstat).

**Testbarhet:** `MainFrame` instansieras aldrig i testsviten idag (`grep -rln "new MainFrame(" app/src` ger bara `AlipsaAccounting.groovy`) eftersom den binder ihop alla tjänster via singleton-fältet `DatabaseService.instance` (satt en gång vid klassladdning, inte per-test-återställningsbar som i `VoucherPanelNavigationTest`s `DatabaseService.newForTesting()`-mönster). Att bygga en fullständig `MainFrame`-testrigg vore oproportionerligt för denna fix. Bryt istället ut selektionslogiken till en liten, ren, statisk hjälpmetod som kan enhetstestas utan Swing/DB:

```groovy
@PackageScope
static FiscalYear resolveFiscalYearToSelect(FiscalYear comboSelection, FiscalYear activeFiscalYear) {
  activeFiscalYear ?: comboSelection
}
```

och byt rad 745 till:

```groovy
FiscalYear selected = resolveFiscalYearToSelect(fiscalYearComboBox.selectedItem as FiscalYear, activeCompanyManager.fiscalYear)
```

Nytt testfall (unit-test, ingen Swing/DB krävs), `app/src/test/groovy/unit/se/alipsa/accounting/ui/MainFrameFiscalYearSelectionTest.groovy`:

```groovy
package se.alipsa.accounting.ui

import static org.junit.jupiter.api.Assertions.assertSame

import org.junit.jupiter.api.Test

import se.alipsa.accounting.domain.FiscalYear

import java.time.LocalDate

final class MainFrameFiscalYearSelectionTest {

  @Test
  void prefersTheActiveCompanyManagersFiscalYearOverTheComboBoxsStaleSelection() {
    FiscalYear staleComboSelection = new FiscalYear(1L, '2029', LocalDate.of(2029, 1, 1), LocalDate.of(2029, 12, 31), false, null)
    FiscalYear newlyCreatedYear = new FiscalYear(2L, '2030', LocalDate.of(2030, 1, 1), LocalDate.of(2030, 12, 31), false, null)

    FiscalYear result = MainFrame.resolveFiscalYearToSelect(staleComboSelection, newlyCreatedYear)

    assertSame(newlyCreatedYear, result)
  }

  @Test
  void fallsBackToTheComboBoxsSelectionWhenThereIsNoActiveFiscalYearYet() {
    FiscalYear staleComboSelection = new FiscalYear(1L, '2029', LocalDate.of(2029, 1, 1), LocalDate.of(2029, 12, 31), false, null)

    FiscalYear result = MainFrame.resolveFiscalYearToSelect(staleComboSelection, null)

    assertSame(staleComboSelection, result)
  }
}
```

(Positional `FiscalYear(id, name, startDate, endDate, closed, closedAt)` constructor — same style already used in `SieExchangeDialogTest.groovy:25-26` and `VoucherEditorActionsTest.groovy:35`, both in the same `unit/.../ui` test directory this new file belongs in.)

Kör testet innan fixen (ska gå grönt trivialt eftersom metoden inte finns än → kompileringsfel/röd), lägg till metoden, kör igen (grönt). Själva wiring:en i `reloadFiscalYearComboBox()` verifieras manuellt/via `./gradlew build` eftersom en fullständig `MainFrame`-integrationstest inte är rimlig att bygga för denna fix (se ovan) — notera detta som en känd, medveten testtäckningslucka.

---

### 2. AI-föreslagen verifikation från PDF: bilaga syns inte, datum kan tystnat ignoreras

Bakgrund: appen parsar inte PDF:er själv — AI-klienten (t.ex. Claude) läser PDF:en och anropar MCP-verktyget `set_active_voucher_draft` (`AccountingMcpTools.groovy:229-262` → `VoucherDraftEditorAccess.setVoucherDraft` → `VoucherPanel.applyDraft()`, `VoucherPanel.groovy:980-1001`). Verktygsbeskrivningen (`McpToolDefinitions.groovy:118-127`) lovar uttryckligen: *"If attachment_path is provided, the file is validated immediately and will be automatically attached to the voucher the moment the user saves it."*

#### 2a. Bilagan syns inte i fliken "Bilagor"

**Fil:** `app/src/main/groovy/se/alipsa/accounting/ui/VoucherPanel.groovy:740` (`showVoucher`)

```groovy
740  private void showVoucher(Voucher v) {
741    currentVoucher = v
742    pendingReceiptAttachmentPath = null
743    balanceCache.clear()
744    if (v == null) {
745      showBlankVoucher()
746      return
747    }
...
768    lineTableModel.setRows(v.lines)
769    ensureAutoRow()
770    recalculateAllBalances()
771    clearAttachmentAndHistory()
772    refreshTotals()
773    applyReadOnlyState()
774    updateNavigationButtons()
775  }
```

`showVoucher()` anropar `clearAttachmentAndHistory()` (rad 771), som bara **tömmer** bilagor-/historik-tabellerna — den laddar aldrig om dem för den nu visade verifikationen. Faktisk omladdning sker bara via `tabs.addChangeListener { refreshAttachmentAndHistory() }` (rad 381), dvs. **enbart när användaren byter flik**. Om användaren redan står på fliken "Bilagor" och sedan bläddrar mellan verifikationer (Föregående/Nästa/Först/Sist/hoppa-till-nummer) utan att byta flik, förblir tabellen tom för varje ny verifikation tills flikbytet triggas manuellt — även om verifikationen faktiskt har en bilaga i databasen. Detta slår igenom rakt av på det rapporterade scenariot: användaren har troligen fliken "Bilagor" öppen just för att verifiera att AI:ns bilaga fästes, sparar, och ser ingenting eftersom inget flikbyte skett.

`refreshAttachmentAndHistory()` (rad 1177-1189) hanterar redan båda fallen korrekt (tömmer om `currentVoucher == null`, annars laddar om innehållet för den flik som faktiskt är vald) — den är en strikt förbättring att använda istället för `clearAttachmentAndHistory()` här.

**Åtgärd:**

```groovy
    lineTableModel.setRows(v.lines)
    ensureAutoRow()
    recalculateAllBalances()
    refreshAttachmentAndHistory()
    refreshTotals()
    applyReadOnlyState()
    updateNavigationButtons()
```

(byt bara `clearAttachmentAndHistory()` → `refreshAttachmentAndHistory()` på rad 771).

**Test** (lägg till i `VoucherPanelNavigationTest.groovy`, samma mönster som `navigatingToASavedVoucherPopulatesNormalBalanceSideForEachLine`):

```groovy
@Test
void attachmentsTabReloadsWhenNavigatingToAnotherVoucherWhileAlreadyOnThatTab() {
  Voucher withAttachment = voucherService.createVoucher(
      fiscalYear.id, 'A', LocalDate.of(2030, 3, 15), 'Har bilaga',
      [voucherLine('1510', 'Kundfordringar', '', 100.00G, 0.00G),
       voucherLine('3010', 'Försäljning', '', 0.00G, 100.00G)]
  )
  Path receipt = tempDir.resolve('kvitto.pdf')
  Files.writeString(receipt, '%PDF-1.4 test')
  attachmentService.addAttachment(withAttachment.id, receipt)
  panel?.dispose()
  panel = buildPanel()
  JTabbedPane tabs = findComponent(panel, JTabbedPane) { true }

  onEdt {
    tabs.selectedIndex = 1
    clickButtonWithTooltip(panel, I18n.instance.getString('voucherPanel.button.prev'))
  }

  assertEquals(1, onEdt { panel.attachmentTableModel.rowCount })
}
```

`attachmentTableModel` är idag `private final AttachmentTableModel attachmentTableModel` (`VoucherPanel.groovy:129`) — byt till `@PackageScope` (samma mönster som `lineTableModel`, rad 126-127) så testet ovan kan läsa `panel.attachmentTableModel.rowCount` under `@CompileStatic`.

Kör testet innan fixen → rött (`rowCount == 0`). Applicera fixen → grönt.

**Designbeslut — det vanliga "spara med AI-bilaga"-flödet (standardinställning "Advance after save" påslagen):**

Fix 2a ovan löser *inte* det vanligaste flödet: skapa förslag via AI, klicka Spara direkt (utan att bläddra först). Orsaken ligger i `handleSavedVoucher()` (`VoucherPanel.groovy:723-738`):

```groovy
723  private void handleSavedVoucher(Voucher savedVoucher) {
724    String attachmentPathToApply = pendingReceiptAttachmentPath
725    pendingReceiptAttachmentPath = null
726    reloadVoucherList(advanceAfterSaveCheckBox.selected ? null : savedVoucher)
727    if (attachmentPathToApply) {
728      attachmentService.addAttachment(savedVoucher.id, Path.of(attachmentPathToApply))
729      refreshAttachmentAndHistory()
730      showInfo(...)
731    }
732  }
```

Med "Advance after save" påslagen (standard, `VoucherPanel.groovy:364-366`) väljer `reloadVoucherList(null)` (rad 726) grenen `showBlankVoucher()`, som sätter `currentVoucher = null` **innan** bilagan ens läggs till. `refreshAttachmentAndHistory()` på rad 729 träffar därför `currentVoucher == null`-grenen och tömmer tabellen — den blankdrafts formuläret som nu visas har korrekt inga bilagor, men det ger ingen bekräftelse om att den *föregående* verifikationens bilaga sparades.

Detta är dock **avsiktligt/rimligt beteende**, inte en ytterligare kodbugg: när UI:t redan gått vidare till nästa tomma utkast (det är precis vad kryssrutan ber om) skulle det vara förvirrande att blanda in föregående verifikations bilageinformation i en flik som annars visar det nya, tomma utkastet. Den verkliga bekräftelsekanalen för just detta ögonblick är den redan existerande statusraden (`showInfo(I18n.instance.format('voucherPanel.message.savedWithReceipt', ...))`, rad 730), som **inte** påverkas av `reloadVoucherList()`s tömning eftersom den körs efteråt, i samma synkrona knapptryck. Denna kedja var dock helt overifierad i planen tidigare — nedanstående test bevisar den, istället för att bara anta den, och gör explicit vad som faktiskt syns var i UI:t vid varje steg i det vanligaste flödet.

**Test** (`VoucherPanelNavigationTest.groovy`) — kör hela vägen: MCP-utkast med bilaga → Spara (standardinställningar) → verifiera DB, statusrad och flik-tillstånd → bläddra tillbaka → verifiera att fliken nu visar bilagan (tack vare fix 2a ovan):

```groovy
@Test
void savingAnAiSuggestedVoucherWithAnAttachmentConfirmsViaToastThenShowsItAfterNavigatingBack() {
  Path receipt = tempDir.resolve('kvitto.pdf')
  Files.writeString(receipt, '%PDF-1.4 test')

  onEdt {
    panel.mcpVoucherDraftAccess.setVoucherDraft([
        accounting_date: '2030-03-20',
        description: 'AI förslag från PDF',
        series_code: 'A',
        attachment_path: receipt.toString(),
        lines: [
            [account_number: '1510', debit: 100.00G, credit: 0.00G],
            [account_number: '3010', debit: 0.00G, credit: 100.00G]
        ]
    ])
    clickButtonWithTooltip(panel, I18n.instance.getString('voucherPanel.button.save'))
  }

  Voucher saved = voucherService.listVouchers(CompanyService.LEGACY_COMPANY_ID, fiscalYear.id).first()
  assertEquals(1, attachmentService.listAttachments(saved.id).size())

  String expectedToast = I18n.instance.format('voucherPanel.message.savedWithReceipt',
      saved.voucherNumber ?: '', receipt.fileName.toString())
  assertEquals(expectedToast, onEdt { panel.feedbackArea.text })
  assertEquals(0, onEdt { panel.attachmentTableModel.rowCount })

  onEdt { clickButtonWithTooltip(panel, I18n.instance.getString('voucherPanel.button.prev')) }

  assertEquals(1, onEdt { panel.attachmentTableModel.rowCount })
}
```

Detta test är oberoende av fix 2a:s kodändring för sina första fyra assertions (DB-persistens och statusrad fungerar redan idag, utan någon av fixarna i detta dokument) — de dokumenterar bara nuvarande, korrekta beteende. Den sista assertionen (`rowCount == 1` efter "Föregående") är den som faktiskt kräver fix 2a; kör testet mot okorrigerad kod för att bekräfta att just den sista raden är den som failar (`rowCount` förblir `0`), inte hela testet av fel anledning.

#### 2b. Datumet fylls inte i korrekt — explicit intervall och MCP-validering, men normalfallet är fortfarande öppet

**Beslut:** den bekräftade tysta felvägen i `DatePicker` är åtgärdad på applikationsnivå på två sätt:

1. `VoucherPanel` skickar explicita gränser till det befintliga publika fyrarguments-anropet: `1900-01-01` till `LocalDate.now().plusYears(50)`. Därmed används inte bibliotekets implicita, fasta ±20-årsintervall. Historiska verifikationer inom det praktiska bokföringsintervallet kan därför visas korrekt i `showVoucher()`.
2. `applyDraft()` kontrollerar datumet före `showBlankVoucher()` och kastar `IllegalArgumentException` med `accounting_date`, datumet och tillåtet intervall om det ligger utanför. Undantaget passerar genom `VoucherDraftEditorAccess`/`SwingThreading` till MCP-klienten, så verktyget rapporterar inte längre falsk framgång. Felet är medvetet ett maskinmeddelande på engelska, inte en lokaliserad UI-sträng.

Detta stänger inte den ursprungligen rapporterade buggen om ett datum som låg inom aktivt räkenskapsår. Ett sådant datum ligger också inom det nya intervallet, så en levande reproduktion med exakt kvittodatum och appens körningstid krävs fortfarande innan 2b kan anses löst i sak. Om datumet var inom intervallet och ändå inte fylldes i korrekt krävs ny felsökning med `set_active_voucher_draft` och loggning av vad `datePicker.date` faktiskt blir efter tilldelningen.

**Filer:**
- `app/src/main/groovy/se/alipsa/accounting/ui/VoucherPanel.groovy` (`applyDraft`, `createDatePicker`, `datePickerAccepts`)
- Externt beroende (används via befintligt publikt API): `se.alipsa:swing-widgets:1.1.2`, `se/alipsa/datepicker/DatePicker.java` (källa i `/home/per/project/swing-widgets`)

`DatePicker` avvisar fortfarande tyst datum utanför sina gränser, och `datePickerAccepts()` speglar därför de konfigurerade `start`/`end`-gränserna före varje MCP-utkast. Den här panelen installerar ingen `vetoPolicy`; om en sådan policy införs senare måste den här kontrollen uppdateras så att den även speglar den policyn.

```groovy
private static final LocalDate DATE_PICKER_RANGE_FROM = LocalDate.of(1900, 1, 1)
private static final LocalDate DATE_PICKER_RANGE_TO = LocalDate.now().plusYears(50)

private static DatePicker createDatePicker() {
  new DatePicker(DATE_PICKER_RANGE_FROM, DATE_PICKER_RANGE_TO, null, I18n.instance.locale)
}
```

**Test** (`VoucherPanelNavigationTest.groovy`): `suggestedDateOutsideThePickersAllowedRangeIsRejectedToTheMcpCaller()` väljer `datePicker.end.plusDays(1)`, anropar `setVoucherDraft`, och verifierar att `IllegalArgumentException.message` innehåller exakt samma maskinmeddelande som produktionskoden. Testet verifierar också den nya kontraktsgränsen: ett avvisat datum försöker inte först applicera ett halvt utkast.

`feedbackArea` och `attachmentTableModel` är package-scoped med kommentarer eftersom integrationstesterna behöver inspektera respektive resultat; de används inte längre för datumfelrapporteringen.

---

### 3. Osparad verifikation försvinner vid bläddring — men bara efter ett AI-förslag

**Fil:** `app/src/main/groovy/se/alipsa/accounting/ui/VoucherPanel.groovy:777-780` (`showBlankVoucher`), `:1064-1082` (`duplicateVoucher`, jämförelse), `VoucherNavigation.groovy:8-25`

Ett skydd för att bevara ett osparat utkast vid bläddring finns redan (commit `8a5309b`) och fungerar för det vanliga fallet — testat och grönt i `preservesUnsavedDraftWhenNavigatingToSavedVouchersAndBack`. Mekaniken bygger på två separata statusflaggor som **måste** hållas synkade:

- `VoucherPanel.currentVoucher` — `null` betyder "visar ett osparat utkast".
- `VoucherNavigation.currentIndex` — `< 0` betyder samma sak (`isOnDraft()`, rad 24-26).

`rememberDraftBeforeNavigation()` (rad 1003-1011) kräver **båda**:

```groovy
private void rememberDraftBeforeNavigation() {
  if (currentVoucher != null || !navigation.isOnDraft()) {
    return
  }
  ...
  navigation.rememberDraft(...)
}
```

`showBlankVoucher()` sätter bara den ena:

```groovy
777  private void showBlankVoucher() {
778    navigation.clearDraft()
779    showEmptyVoucher()     // sätter currentVoucher = null
780  }
```

— den anropar **inte** `navigation.showDraft()` (som sätter `currentIndex = -1`). Jämför med `duplicateVoucher()` (rad 1064-1082), som gör det rätt:

```groovy
1071    navigation.showDraft()
1072    showBlankVoucher()
```

`applyDraft()` (MCP-vägen, `set_active_voucher_draft` → `VoucherDraftEditorAccess.setVoucherDraft` → `draftConsumer.accept(voucherDraft)` → `applyDraft`) anropar **bara** `showBlankVoucher()`, aldrig `navigation.showDraft()`:

```groovy
980  private void applyDraft(VoucherDraftMapper.VoucherDraft voucherDraft) {
     ...
991    showBlankVoucher()
992    datePicker.date = voucherDraft.accountingDate
     ...
```

**Reproduktionsscenario (bekräftat via kodläsning, inte bara test):**
1. Användaren bläddrar till en tidigare sparad verifikation (t.ex. index 1). `currentVoucher` = den verifikationen, `navigation.currentIndex = 1`.
2. AI:n föreslår en ny verifikation från en PDF medan panelen fortfarande visar den sparade verifikationen (`set_active_voucher_draft` anropas). `applyDraft()` körs: `currentVoucher` blir `null` (utkastläge), men `navigation.currentIndex` **förblir `1`** — de två statusflaggorna är nu osynkade.
3. Användaren klickar "Föregående" för att titta på en äldre verifikation. `rememberDraftBeforeNavigation()` ser `currentVoucher == null` (sant) **men** `navigation.isOnDraft()` är `false` (`currentIndex` är fortfarande `1`, inte `-1`) → villkoret `!navigation.isOnDraft()` är sant → metoden returnerar **utan** att spara utkastet. AI:ns föreslagna verifikation är nu permanent borta ur `navigation`s minne.
4. Användaren bläddrar tillbaka ("Nästa" tillräckligt många gånger) till vad som borde vara utkastplatsen — men eftersom inget utkast sparades i steg 3 visas antingen en annan sparad verifikation eller ett tomt formulär, aldrig AI:ns förslag.

Detta matchar exakt det rapporterade beteendet, och förklarar varför det befintliga, redan testade skyddet inte fångade det: det befintliga testet utlöser aldrig steg 2 (MCP-utkast tillämpat medan `currentIndex` redan pekar på en sparad verifikation).

**Åtgärd:** håll invarianten "currentVoucher == null ⇔ navigation.isOnDraft() == true" på det enda stället där den bryts — inne i `showBlankVoucher()` självt, så att *alla* anropare (inklusive `applyDraft()`/MCP-vägen) automatiskt får rätt beteende:

```groovy
private void showBlankVoucher() {
  navigation.clearDraft()
  navigation.showDraft()
  showEmptyVoucher()
}
```

(`duplicateVoucher()`s explicita `navigation.showDraft()`-anrop på rad 1071 blir därmed överflödigt men ofarligt att låta stå kvar — ta inte bort det i denna fix för att hålla diffen minimal och fokuserad.)

**Test** (`VoucherPanelNavigationTest.groovy`, mönster efter `preservesUnsavedDraftWhenNavigatingToSavedVouchersAndBack`):

```groovy
@Test
void aiSuggestedDraftAppliedWhileViewingASavedVoucherSurvivesBrowsingAwayAndBack() {
  voucherService.createVoucher(
      fiscalYear.id, 'A', LocalDate.of(2030, 2, 1), 'Äldre sparad verifikation',
      [voucherLine('1510', 'Kundfordringar', '', 100.00G, 0.00G),
       voucherLine('3010', 'Försäljning', '', 0.00G, 100.00G)]
  )
  voucherService.createVoucher(
      fiscalYear.id, 'A', LocalDate.of(2030, 3, 1), 'Nyare sparad verifikation',
      [voucherLine('1510', 'Kundfordringar', '', 200.00G, 0.00G),
       voucherLine('3010', 'Försäljning', '', 0.00G, 200.00G)]
  )
  panel?.dispose()
  panel = buildPanel()
  JTextField description = findComponent(panel, JTextField) { JTextField field -> field.columns == 30 }

  onEdt {
    clickButtonWithTooltip(panel, I18n.instance.getString('voucherPanel.button.prev'))
    panel.mcpVoucherDraftAccess.setVoucherDraft([
        accounting_date: '2030-04-01',
        description: 'AI förslag från PDF',
        series_code: 'A',
        lines: [
            [account_number: '1510', debit: 150.00G, credit: 0.00G],
            [account_number: '3010', debit: 0.00G, credit: 150.00G]
        ]
    ])
    clickButtonWithTooltip(panel, I18n.instance.getString('voucherPanel.button.prev'))
    clickButtonWithTooltip(panel, I18n.instance.getString('voucherPanel.button.next'))
  }

  assertEquals('AI förslag från PDF', onEdt { description.text })
  assertEquals('1510', onEdt { panel.lineTableModel.rows[0].accountNumber })
}
```

Kör testet innan fixen → rött (beskrivningen är en av de sparade verifikationernas, inte AI-förslaget). Applicera fixen → grönt.

## Kritiska filer

- `app/src/main/groovy/se/alipsa/accounting/ui/ActiveCompanyManager.groovy:115-119` (bugg 1a) — `reloadFiscalYears(Long preferredFiscalYearId = null)`.
- `app/src/main/groovy/se/alipsa/accounting/ui/FiscalYearPanel.groovy:264` (bugg 1a) — skicka `year.id` vidare.
- `app/src/test/groovy/unit/se/alipsa/accounting/ui/ActiveCompanyManagerTest.groovy` — två nya testfall (bugg 1a).
- `app/src/main/groovy/se/alipsa/accounting/ui/MainFrame.groovy:745` (bugg 1b) — nytt `resolveFiscalYearToSelect`, `@PackageScope static`.
- `app/src/test/groovy/unit/se/alipsa/accounting/ui/MainFrameFiscalYearSelectionTest.groovy` — ny testfil (bugg 1b).
- `app/src/main/groovy/se/alipsa/accounting/ui/VoucherPanel.groovy:775` (bugg 2a), `:727-742` (bugg 2a, verifiering av befintligt beteende), `:999-1010` (bugg 2b, MCP-validering), `:781-785` (bugg 3).
- `app/src/main/groovy/se/alipsa/accounting/ui/VoucherPanel.groovy:108,129` — `feedbackArea` och `attachmentTableModel` byter till `@PackageScope` för testbarhet.
- `app/src/test/groovy/integration/se/alipsa/accounting/ui/VoucherPanelNavigationTest.groovy` — fem nya testfall (bugg 2a × 2, 2b, bilage-/historikflikarna, 3).

## Verifiering

- Varje deltagg fixas TDD: skriv testet, kör det och bekräfta att det failar av rätt anledning, applicera minimal fix, kör igen och bekräfta grönt.
- `./gradlew test --tests "se.alipsa.accounting.ui.ActiveCompanyManagerTest" --tests "se.alipsa.accounting.ui.MainFrameFiscalYearSelectionTest" --tests "se.alipsa.accounting.ui.VoucherPanelNavigationTest"` efter alla deltasteg.
- `./gradlew spotlessApply` följt av manuell diff-granskning (Spotless rör ibland även Markdown).
- `./gradlew codenarcMain` för de ändrade produktionsklasserna (`ActiveCompanyManager.groovy`, `FiscalYearPanel.groovy`, `MainFrame.groovy`, `VoucherPanel.groovy`).
- `./gradlew build` innan PR öppnas, per `CLAUDE.md`.
- Ingen schema-/migrationspåverkan i denna bugfix-batch.
- Manuell rök-test i den riktiga appen rekommenderas för:
  - bugg 1: skapa ett bakåtdaterat räkenskapsår (tidigare startdatum än ett redan existerande år) och bekräfta att både `MainFrame`s kombobox och resten av appen (t.ex. `VoucherPanel`) direkt växlar till det nya året, inte till det som redan hade senaste startdatumet.
  - bugg 2a: spara en AI-föreslagen verifikation med bilaga med standardinställningar och bekräfta statusradens text; navigera sedan tillbaka till den och bekräfta att fliken "Bilagor" nu visar filen.
  - bugg 2b (specifikt, eftersom rotorsaken för normalfallet inte är bekräftad — se "Beslut" i 2b): skapa en verifikation via `set_active_voucher_draft` med ett datum *inom* räkenskapsåret och bekräfta att det fortfarande fylls i korrekt (ingen regression), **samt** försök reproducera det ursprungligen rapporterade symptomet med det faktiska datum som användes då. Om det fortfarande går fel med ett datum inom intervallet är bugg 2b inte löst av denna batch och kräver uppföljning.

## Explicit utanför scope

- Att ändra implementationen i det externa biblioteket `se.alipsa:swing-widgets` (`DatePicker`) självt, t.ex. att göra dess intervall dynamiskt kopplat till aktivt räkenskapsår eller kasta ett fel istället för tyst no-op — appen använder i denna batch det befintliga publika API:et med explicita gränser; en bibliotekspatch ligger fortsatt utanför scope.
- En riktig rotorsaksfix för bugg 2b om den visar sig gälla datum *inom* det tillåtna intervallet — se "Beslut" under 2b; kräver reproduktion innan den kan specificeras.
- Att ändra `reloadFiscalYears()`s "senaste startdatum"-fallback för `FiscalYearPanel`s stäng-/återöppna-/ta bort-flöden (rad 310/355/371) till att i första hand behålla samma år om det fortfarande finns — närliggande men orapporterat; noteras i `req/roadmap.md` som en framtida förbättring, inte en del av denna bugfix-batch.
- En "väntande bilaga"-indikator i fliken "Bilagor" *innan* en AI-föreslagen verifikation sparas (dvs. visa den ännu-inte-persisterade PDF-filen som en rad innan Spara klickas). Det vanliga spara-flödet är nu explicit dokumenterat och testat i 2a (statusraden är bekräftelsekanalen där, fliken visar det nya tomma utkastet); en väntande-indikator är en UX-utökning, inte en bugfix, och läggs i `req/roadmap.md` som en framtida förbättring om den fortsatt efterfrågas.
- Generell refaktorering av `VoucherPanel.groovy` (redan nära CodeNarc:s klassstorleksgräns enligt tidigare cleanup, se `req/v1.4.0-cleanup.md`) — endast de punktvisa raderna ovan ändras.
